export function FixtureBanner() {
  return (
    <div className="fixture-banner" role="status" aria-live="polite" data-testid="fixture-banner">
      Displaying fixture data — not connected to live CLI
    </div>
  );
}
