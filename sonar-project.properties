# Identity + scope only. Quality Gate thresholds live in SonarCloud — do not override here.
# Do NOT add local coverage/rating minimums that diverge from the SonarCloud Quality Gate.
sonar.projectKey=bayly-ai_HATH0R-POC
sonar.organization=bayly-ai
sonar.projectName=HATH0R-POC
sonar.sources=src,lib
sonar.tests=test
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/coverage/**,**/htmlcov/**,**/.hath0r/**,**/packaging/**,**/.git/**
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.tsconfigPath=tsconfig.json
sonar.sourceEncoding=UTF-8
